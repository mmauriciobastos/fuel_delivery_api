api_platform:
    title: Fuel Delivery Management API
    description: Multi-tenant SaaS API for fuel delivery operations
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
    formats:
        jsonld: ['application/ld+json']
        json: ['application/json']
        html: ['text/html']
